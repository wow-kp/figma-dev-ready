{
  "name": "figma-dev-ready",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "esbuild src/main.ts --bundle --outfile=code.js --format=iife --target=es2017 --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.27.4"
  }
}
